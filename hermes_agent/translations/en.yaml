configuration:
  config_yaml:
    name: Hermes configuration YAML
    description: Native Hermes config.yaml. This editor reflects /opt/data/config.yaml; saving changes updates the native file and restarts Hermes.
